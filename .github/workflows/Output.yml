name: Output Information
on: workflow_dispatch
jobs: 
  if: ${{ github.workflow == "Deploy website" }}
  output:
    runs-on: ubuntu-latest
    steps: 
      - name: output github context
        run:  echo "${{ toJSON(github) }}"
      - name: output log
        run: echo "Goodbye"

